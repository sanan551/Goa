/*
JavaScript-ს ორი ძირითადი მეხსიერების ტიპი აქვს:
1) Stack (სტეკი) → პატარა, სწრაფი მეხსიერება.
   აქ ინახება პრიმიტიული ტიპები:
   - Number
   - String
   - Boolean
   - Undefined
   - Null
   - Symbol
   პრიმიტივები **immutable** (ცვლილებადი არ არის).

2) Heap (ჰიპი) → დიდი, ნელი მეხსიერება.
   აქ ინახება კომპლექსური (Reference) ტიპები:
   - Object
   - Array
   - Function
   კომპლექსურ ტიპებს JS სტეკში მხოლოდ **ბმული (reference)** ინახება,
   ხოლო რეალური მონაცემები ჰიპშია.

📌 მაგალითი:
*/
let a = 10;          // პრიმიტივი → ინახება Stack-ში პირდაპირ
let b = "Hello";     // პრიმიტივი → Stack
let c = [1, 2, 3];   // Array → Stack-ში მხოლოდ reference ინახება, რეალური მასივი Heap-შია
let d = {x: 5};      // Object → Heap-ში ინახება, Stack-ში → reference

/*
თუ პრიმიტივს გადავაწერთ მნიშვნელობას → რეალურად იქმნება ახალი მნიშვნელობა.
თუ ობიექტს გადავაწერთ → ორივე ცვლადი ერთსა და იმავე Heap მონაცემს მიუთითებს.
*/
